const getters = {
  GetPrograms: state => state.programs,
  SubjectRegistrations: state => state.subject_registrations,
  SubjectRegistration: state => state.subject_registration,
  Messages: state => state.messages,
  Exam: state => state.exam,
}
export default getters
